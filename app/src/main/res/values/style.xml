<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppThemeLight" parent="MaterialDrawerTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlActivated">@color/colorPrimary</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="popupTheme">@style/ThemeActionMenu</item>

        <!-- MaterialDrawer specific values -->
        <item name="material_drawer_background">@color/material_drawer_background</item>
        <item name="material_drawer_primary_text">@color/white</item>
        <item name="material_drawer_primary_icon">@color/white</item>
        <item name="material_drawer_secondary_text">@color/white</item>
        <item name="material_drawer_hint_text">@color/white_hint</item>
        <item name="material_drawer_divider">@color/white_hint</item>
        <item name="material_drawer_selected">@color/white</item> <!-- Material 2 defines 12% alpha, primary color -->
        <item name="material_drawer_selected_legacy">@color/white</item> <!-- Defines the color if legacy style (Material 1, is enabled) -->
        <item name="material_drawer_selected_text">@color/white</item>
        <item name="material_drawer_header_selection_text">@color/white</item>
        <item name="material_drawer_header_selection_subtext">@color/white</item>
        <item name="material_drawer_legacy_style">@color/white</item> <!-- Enables legacy Material 1 style -->
    </style>

    <style name="ThemeActionMenu">
        <item name="background">@color/brown</item>
        <item name="actionMenuTextColor">@color/white</item>
    </style>

</resources>